#ifndef CADENA_H
#define CADENA_H

#include <stdio.h>
#include <stdlib.h>

int esPalindromo(const char *cadena);
int verificarParentesis(const char *cadena);
void ordenarPila(const char *cadena);

#endif